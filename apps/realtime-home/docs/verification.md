# v0.1 验证记录（迁移前）

下文保留首次交付时实际使用的 npm 命令与结果。当前 pnpm workspace 的验证结果见 [pnpm monorepo 迁移记录](../../../docs/pnpm-monorepo.md)。

2026-09-18 后续已接通真实 TypeSafe 与 DeepSeek；真实调用记录见 [真实模型联调](../../../docs/live-integration.md)。下文“未配置真实密钥”仅描述首次交付时的历史情况。

本记录覆盖 `apps/realtime-home/` 中的独立实现。验证使用 Node.js 26.5.0、本机 Chrome 和本地规则模式，没有消耗真实模型额度。

## 已执行并通过

| 检查 | 结果 |
| --- | --- |
| `npm test` | 4 个测试文件，29 项全部通过 |
| `npm run build` | TypeScript 严格检查通过，Vite 生产构建成功 |
| `npm run test:e2e` | 3 项真实浏览器测试全部通过 |
| 桌面与 390px 截图 | 已人工查看，3D 画面正常、手机无横向溢出 |

运行时测试涵盖所有交互点之间的无穿墙路径、动作抵达与完成条件、实际需求变化、顺序任务、停止保持、新请求复用当前动作、暂停、快慢响应过期、低置信度保护、只有快系统可以请求慢思考、偏好采纳与记忆保存。

HTTP 契约测试涵盖 Jev Choice/Noul 请求及响应结构、置信度与概率的区分、非法动作/分布拒绝、429 与 Retry-After、错误内容净化、响应体上限、慢思考 JSON 校验、消息输入校验、家具请求走相同意图入口、同源限制与 SSE 快照。

浏览器测试实际操作输入框与按钮，验证“先喝水，再给植物浇水”的执行结果、睡觉期间停止、记忆面板、快系统发起并采纳模拟慢思考、家具选择、暂停、模型配置说明与移动端布局。

## 实际边界

本次未配置真实 TypeSafe 或 LLM 密钥。真实适配器经过 HTTP fixture 测试，实际模型质量、线上响应延迟、额度消耗与提供商兼容性仍待账户联调。演示中 Jev/LLM 请求计数保持为 0，不把本地规则的耗时展示为真实模型推理性能。

浏览器麦克风识别与朗读使用浏览器提供的能力，本次没有验证物理麦克风输入及扬声器输出。移动布局在 Chrome 390px 视口验证，没有据此宣称所有手机 GPU 或浏览器兼容。

构建包含一个约 560 kB 的 Three.js 分块（gzip 约 139 kB），触发 Vite 的 500 kB 体积提示；构建成功，没有提高警告阈值来隐藏该提示。

浏览器截图保存在本机 `test-results/home-desktop.png` 和 `test-results/home-mobile.png`。该目录被 Git 忽略。
