# Explicit opt-in: demo uses local rules; live uses Jev for every decision.
AGENT_MODE=demo
PORT=3102
TYPESAFE_API_KEY=
TYPESAFE_MODEL=jev-latest

# Optional slow thinker, called ONLY when the fast decision requests it.
LLM_BASE_URL=https://api.openai.com/v1
LLM_API_KEY=
LLM_MODEL=
